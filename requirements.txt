mcp[cli]>=1.28,<2.0
pydantic>=2.0,<3.0
pywinauto>=0.6.8,<0.7
Pillow>=9.0,<12.0
# Dev / CI only — kept here so a fresh `pip install -r requirements.txt` on a
# CI runner (where e2e tests will eventually live) has everything it needs.
pytest>=8.0,<9.0